import { StyleSheet } from "react-native";

export default StyleSheet.create({
    HomeScreen: {
        flex: 1,
        textAlign: 'center',
        alignItems: 'center',
        justifyContent: 'center',
    }
})